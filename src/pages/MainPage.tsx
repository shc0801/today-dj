import React from 'react';
import styled from 'styled-components';
import { Span, Img } from '../components/atomic'

const Container = styled.div`
  width: 100%;
`;

const MainPage: React.FC = () => {
  return <Container>asd</Container>;
};

export default MainPage;
